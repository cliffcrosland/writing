{
  "name": "frontend",
  "version": "0.1.0",
  "dependencies": {
    "@testing-library/jest-dom": "^5.12",
    "@testing-library/react": "^11.2",
    "@testing-library/user-event": "^12.8",
    "@types/jest": "^26.0",
    "@types/node": "^12.20",
    "@types/react": "^17.0",
    "@types/react-dom": "^17.0",
    "react": "^17.0",
    "react-dom": "^17.0",
    "react-scripts": "4.0",
    "typescript": "^4.2",
    "web-vitals": "^1.1"
  },
  "private": true,
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject",
    "build-wasm-dev": "wasm-pack build ./wasm --target bundler --out-dir ../src/wasm/pkg --dev",
    "build-wasm-release": "wasm-pack build ./wasm --target bundler --out-dir ../src/wasm/pkg --release"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.4.0",
    "react-app-rewired": "^2.1.8"
  }
}
